# vscode
.vscode

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled main.js file in the repo.
# They should be uploaded to GitHub releases instead.
main.js

# Build intermediate: esbuild-svelte's raw CSS output before it's combined
# into styles.css (the file Obsidian actually loads). Not a shipped asset.
main.css

# Exclude sourcemaps
*.map

# obsidian
data.json

# Exclude macOS Finder (System Explorer) View States
.DS_Store

# Agent working docs
.agent/
AGENTS.md

# test coverage reports
coverage/